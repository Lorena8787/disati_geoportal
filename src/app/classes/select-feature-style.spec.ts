import { SelectFeatureStyle } from './select-feature-style';

describe('SelectFeatureStyle', () => {
  it('should create an instance', () => {
    expect(new SelectFeatureStyle()).toBeTruthy();
  });
});
